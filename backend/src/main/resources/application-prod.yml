# 生产环境配置 —— 启动时加 --spring.profiles.active=prod
# 敏感信息通过环境变量注入，不再使用默认值

mybatis-plus:
  configuration:
    # 生产环境关闭 SQL 日志输出
    log-impl: org.apache.ibatis.logging.nologging.NoLoggingImpl

logging:
  level:
    com.example.lostfound: WARN
    com.baomidou.mybatisplus: WARN
