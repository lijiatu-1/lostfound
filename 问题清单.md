# 失物招领平台 — 业务逻辑与边界问题清单

> 最后更新: 2026-05-12

---

## 一、后端业务逻辑问题 ✅ 全部修复

### 1. 申请重复提交无检测 ✅
**修复**: `applyAction` 调用 `findByItemAndApplicant()`，重复则拒绝。

### 2. 认领已解决的物品未拒绝 ✅
**修复**: `applyAction` 校验 `item.getStatus().equals("active")`。

### 3. 标记已解决时未自动拒绝其他申请 ✅
**修复**: `handleApplication` accept 时调用 `rejectOtherApplications()`。

### 4. 处理申请时未通知申请人 ✅
**修复**: accept 时发"申请已通过"，reject 时发"申请已被拒绝"。

### 5. 编辑物品可修改已过期物品 ✅
**修复**: `editItem` 增加 `item.getStatus().equals("active")` 校验。

### 6. 全局异常处理器顺序问题 ✅
**修复**: `IllegalArgumentException` 移到 `Exception` 前面。

### 7. null 用户未返回 404 ✅
**修复**: `updateProfile` 增加 null 用户检查。

### 8. 重复处理已拒绝/已通过的申请 ✅
**修复**: `handleApplication` 检查 `application.getStatus().equals("pending")`。

### 9. action 参数校验 ✅
**修复**: 校验 action 必须为 accept 或 reject。

---

## 二、前端业务逻辑问题 ✅ 全部修复

### 1. 全局 userId 类型不一致 ✅
**修复**: `app.js` 中 `Number(res.user.id)`。

### 2. index 页重复加载 ✅
**修复**: onShow 中 `if (items.length === 0)` 才加载。

### 3. timeago 精度不足 ✅
**修复**: 所有页面 timeago 阈值统一为 30 天。

### 4. reverseGeocode 拼接 undefined ✅
**修复**: `[city, district, street].filter(Boolean).join('')`。

---

## 三、边界条件处理 ✅ 全部修复

### 1. 分页参数校验 ✅
**修复**: `page >= 1`，`pageSize ∈ [1, 100]`。

### 2. 内容长度校验 ✅
**修复**: title ≤ 100，description ≤ 500，application content ≤ 500。

### 3. type 值校验 ✅
**修复**: item.type "lost"/"found"，application.type "help"/"claim"。

### 4. 重复已读操作 ✅
**修复**: `markAsRead` 检查 `message.getIsRead()`，已读则跳过。

### 5. 删除物品级联处理 ✅
**修复**: `deleteItem` 先调用 `applicationService.deleteByItemId()` 级联删除。

### 6. 昵称空字符串 ✅
**修复**: `updateProfile` 校验 nickname 非空。

---

## 四、安全问题 ✅ 全部修复

### 1. itemId 类型转换异常 ✅
**修复**: 校验 `request.get("itemId") != null`。

### 2. Authorization Header 缺失 ✅
**修复**: `NullPointerException` 处理器返回 401。

### 3. 大小写匹配绕过 ✅
**修复**: type 校验用白名单，大写值直接拒绝。

---

## 五、数据库问题 ✅ 已修复

### 1. images/tags 格式统一 ✅
**修复**: DataInitializer tags 改为 JSON 数组格式 `["耳机","图书馆","白色"]`。

### 2. Server Timezone
低优先级，当前仅在国内使用。

---

## 修复统计

| 类别 | 总数 | 已修复 |
|------|------|--------|
| 后端逻辑 | 9 | 9 |
| 前端逻辑 | 4 | 4 |
| 边界条件 | 6 | 6 |
| 安全 | 3 | 3 |
| 数据库 | 2 | 1 |
| **合计** | **24** | **23** |
