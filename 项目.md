# 校园失物招领微信小程序 — 需求与里程碑梳理

---

## 一、项目定位
校内实名制失物招领平台（微信小程序），包含三个页面：
- 失物公示（首页）
- 失物发布
- 个人中心

核心流程：用户可发布寻物 / 招领信息 → 公开公示 7 天 → 其他用户可搜索、查看、帮助或申请认领 → 双方通过消息沟通 → 所有发布与认领需完成校园卡实名认证。

---

## 二、页面功能详情

### 1. 失物公示页（首页）
- 顶部搜索框（文字搜索）
- 三个分类标签：`全部`（默认选中，绿色下划线）、`寻物`、`招领`
- 信息卡片列表，每条包含：
  - 左侧物品图片
  - 右侧：标题、红色“寻物”/绿色“招领”标签、地点、发布时间（相对时间）
- 右下角绿色悬浮 `+` 按钮（快捷发布入口）
- 底部导航：失物公示（选中绿色）

### 2. 失物发布页
- 拍照上传区：虚线框，点击调起摄像头或相册
- 基本信息表单：
  - 类型（`寻物 >` 可切换）
  - 标题输入框
  - 地点输入框
  - 描述文本域
- 位置定位模块：显示当前地址，右侧“重新定位”按钮
- 全宽绿色“发布”按钮
- 底部导航：失物发布（选中绿色）

### 3. 个人中心页
- 顶部绿色用户信息卡片：头像、昵称、认证状态
- 数据指标：我的发布、我的认领、消息（红色未读角标）
- 待处理通知区：闪电图标，实时动态列表
- 功能菜单：校园卡认证、隐私设置、帮助与反馈、关于我们
- 底部导航：个人中心（选中绿色）

---

## 三、核心业务规则

| 项目 | 说明 |
|------|------|
| 信息类型 | 寻物（我丢了） / 招领（我捡到） |
| 信息有效期 | 发布后公开显示 **7 天**，到期后仅发布者自己可见 |
| 详情页按钮 | 寻物 → “帮助”（可提供线索）；招领 → “申请认领”（提交证明材料） |
| 消息通知 | 帮助线索、认领申请均以消息形式通知发布者，支持未读角标 |
| 认证要求 | 必须完成 **校园卡认证**（学号+校园卡信息，管理员审核）才能发布、认领或帮助 |
| 个人发布管理 | 我的发布列表可查看所有自己的信息（含已隐藏），支持重新编辑 |
| 认领记录 | 我的认领展示发起过的认领申请及其状态 |

---

## 四、功能里程碑（分阶段）

### MVP（最小可行版本）目标
> 实现核心闭环：发布、搜索（文字）、帮助/认领、个人中心、认证，但不涉及自动定位、模型识图与跨模态搜索。

- **发布功能**：拍照上传（不调用识图 API），表单手动填写，发布类型、标题、地点、描述。
- **位置信息**：暂时不含自动定位，可手动输入地点或使用预设地址，强化版本再加定位。
- **搜索功能**：纯文本搜索，匹配标题、描述、地点等字段，按标签特征筛选。
- **详情与互动**：展示完整信息，提供“帮助”或“申请认领”按钮，发送文字证明/线索。
- **消息系统**：接收帮助/认领通知，列表展示，未读标记。
- **个人中心**：我的发布（含编辑）、我的认领、消息列表、认证申请入口。
- **认证模块**：提交校园卡信息，管理员审核，认证通过后才允许发布与互动权限。
- **信息生命周期**：7 天自动隐藏。

### 强化版本目标
> 在 MVP 基础上增强体验与智能化，加入定位、跨模态搜索、模型识图。

- **自动定位与重定位**：
  - 发布页自动获取当前地理位置，逆地址解析显示详细地址（如“湖北经济学院”）。
  - 支持点击“重新定位”重新获取位置。
- **跨模态搜索**：
  - 用户输入文字（如“蓝色耳机”），可搜索到相关图片特征相似的信息。
  - 实现文本→图片的向量检索，需提前将所有物品图片入库并向量化。
- **一键识图自动填表**：
  - 拍照上传后，调用多模态大模型 API（如 GPT-4V 等）自动识别物品的颜色、类型、特征等。
  - 识别结果自动填入“描述”文本域，用户可修改后发布。

---

## 五、推荐实施路径
1. **MVP 阶段**  
   - 搭建小程序框架，实现三页面 UI。  
   - 开发后端 API（发布、列表、详情、搜索、帮助/认领、消息、认证）。  
   - 信息存储采用云数据库，图片使用云存储。  
   - 测试并上线基础闭环。
2. **强化阶段**
   - 集成地图 SDK，实现自动定位与重定位。
   - 接入向量数据库（如 Milvus）并写入图片 embedding，配合 CLIP 模型实现跨模态搜索。
   - 对接多模态模型 API，完成一键识图并回填描述。

---

## 六、注意事项
- MVP 阶段搜索仅为文本匹配，字段索引设计要做好。
- 认证审核需要后台管理页（可先简易实现）。
- 7 天隐藏逻辑可通过定时任务或查询时过滤 `created_time` 实现。
- 强化阶段的识图与搜索功能需评估 API 成本与响应时间。

---

## 七、边界问题与设计决策

### 1. 身份认证与审核的边界
**推荐方案：先发布，审核通过后公开可见**
- **未认证用户权限**：仅可浏览公示页，不能发布、认领、发送消息。点击发布/认领按钮时直接拦截并引导至认证页面。
- **认证审核流程**：
  - 用户提交：学号、姓名、校园卡正面照片（可要求手持校园卡拍照，人脸与校园卡同框）。
  - 管理员异步审核，MVP 阶段可借助云开发后台管理页手动处理。
  - 审核标准：照片清晰可辨，姓名学号与填写一致。
- **防冒用措施**：
  - 同一学号仅能绑定一个微信 openid。
  - 有条件时可对接学校教务/一卡通系统验证学号真实性，暂时无法对接则依靠人工审核加手持照片提高冒用难度。

### 2. 信息时效与可见性的边界
**推荐：以发布时间为准，支持手动延期一次**
- **“7天”起算点**：统一以信息发布时间为准。
- **过期后处理**：
  - 状态变为“已过期”，从公示页隐藏，但仍保留在“我的发布”列表中（标记灰色“已过期”标签）。
  - 已发起的认领申请和帮助线索仍可继续处理，消息通道不关闭。
  - 允许发布者手动延期一次（+7天），延期后信息回到公示页。
- **重新发布**：超过14天仍未找到可“一键重发”，生成新记录（原记录保留）。
- **UI 区分**：正常信息白色卡片，过期信息灰色/半透明并显示“已过期”角标。

### 3. 认领与帮助功能的交互边界
**认领申请机制**
- **证明材料要求**：至少提供文字描述（何时何地丢失、物品特征），可选择性上传图片（购买记录、历史照片）。不上传图片不禁止提交，但系统建议提供以增加可信度。
- **多人申请**：一个招领信息允许多人申请，发布者收到后需手动“确认失主”，确认后其他申请自动拒绝；状态包括待确认、已确认失主、已拒绝。
- **反恶意认领**：同一用户对同一物品只能申请一次，被拒绝后不可重复申请。
- **帮助/线索形式**：私信形式。点击“帮助”后弹出文本输入框与可选图片上传，提交后以消息形式发送给发布者。
- **前置条件**：双方均需认证通过后才能使用消息功能。
- **已找回标记**：发布者可主动标记“已找回”，信息从公示页移除，相关申请/线索自动关闭并通知参与者。其他用户亦可举报“疑似已找回”，由管理员介入。

### 4. 搜索功能的边界
- **搜索范围**：标题、描述、地点，不包含发布者昵称（保护隐私）。
- **匹配规则**：MVP 阶段采用模糊匹配（如 LIKE 或文本索引），搜索“蓝牙”能匹配到“蓝牙耳机”。
- **分类标签与搜索的关系**：
  - 在“寻物”标签下搜索，仅搜寻物类信息；在“招领”下仅搜招领。
  - 切换标签时保留搜索词，重新过滤；清空搜索框后恢复默认全部信息流。

### 5. 定位与隐私
**定位获取与降级**
- 调用 `wx.getLocation` 获取经纬度，逆地址解析显示文字地址。
- 用户拒绝授权时，提供手动输入地点或从预设校内地点列表中选择（如“图书馆”“一食堂”等）。
- 降级优先级：自动定位 → 预设地点选择 → 自由文本输入。

**隐私保护**
- 地址精度控制在楼栋级，提示用户勿填写具体楼层或宿舍号。
- 输入框旁添加温馨提示：“为保护隐私，建议地点精确到楼栋即可。”

### 6. 图片与内容审核
- **图片限制**：仅限 JPG/PNG，单张不超过 5MB，前端上传前自动压缩至宽度 1024px。
- **内容安全**：接入微信云开发内容安全 API（`security.imgSecCheck` + `security.msgSecCheck`）自动检测违规图片和文本，敏感内容进入待审核队列，管理员可后台处理。

### 7. 消息与通知系统
- **已读/未读机制**：点进消息列表不改变已读状态，进入具体会话后该会话内所有消息标记为已读。角标数字为所有会话未读总数。
- **消息保留**：信息过期后历史消息仍保留，用户可查看；不支持删除，但支持“归档”（标记已处理并折叠）。
- **沟通渠道**：帮助/认领消息为单向通知+简单对话，发布者可在消息中心内直接回复，形成轻量 1v1 对话，不额外开发即时聊天。

### 8. 数据一致性与状态同步
- **信息编辑影响**：发布者可编辑标题、描述、地点，但不可修改类型（寻物/招领）和图片。编辑后系统自动向相关参与者发送通知：“发布者更新了物品信息，请留意”。
- **状态变更同步**：标记“已找回”后，信息移出公示页，所有待处理认领申请自动拒绝，并通知所有参与者。标记操作不可逆，误标记需联系管理员后台恢复。

### 9. 权限与账号体系
- **用户范围**：MVP 阶段仅限在校学生（凭学号+校园卡认证）。后续可扩展“教职工”角色。非学生访客捡到物品可通过“访客提交”入口，由管理员代发，标记为“访客提交”。
- **账号绑定**：同一学号仅绑定一个微信 openid，更换微信需重新认证并由管理员解绑原账号。管理员后台提供手动解绑功能。

### 10. 冷启动与运营
- **冷启动**：预置 3-5 条“示例信息”（标注“示例”，不可认领）；空状态展示引导文案“还没有人发布信息？快来发布第一条吧！”；与学生会/权益部合作录入真实种子数据。
- **举报功能**：详情页右上角提供“举报”入口，预设原因：信息不实、物品已找回但未标记、不当内容、骚扰等。举报进入管理员后台处理队列，可下架信息或冻结用户发布权限。

---

## 八、后续待定事项
- 数据库集合设计
- API 接口清单
- 管理员后台功能详细设计
- 消息推送模板与策略
- 图片向量化与跨模态搜索具体实现方案

---

*以上内容整合了初始需求梳理与十大边界问题的设计决策，可直接用于项目启动和开发参考。*