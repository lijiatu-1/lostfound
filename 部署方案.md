# 失物招领平台 — 阿里云部署方案

> 面向本科学生，总费用约 100-150 元/月（含优惠）

---

## 一、服务器方案

### 推荐：1 台 ECS 自建全部服务

| 配置 | 规格 | 月费参考 |
|------|------|---------|
| CPU/内存 | 2核 4GB（t6 突发型） | ~60-80 元 |
| 系统盘 | 40GB ESSD | 含在套餐内 |
| 带宽 | 1Mbps 按固定带宽 | ~23 元 |
| OS | Alibaba Cloud Linux 3 / Ubuntu 22.04 | 免费 |

**学生优惠**：
- 阿里云「飞天加速计划」学生机 9.9 元/月起（2C2G）
- 但 2G 内存跑 Java + MySQL 较紧，建议至少 4G
- 先用学生机试跑，不够再升级

### 软件部署（全部装在一台机器上）

```
Nginx（反向代理 + HTTPS）
├── /api/* → Spring Boot（8080端口）
├── /images/* → 静态文件（或 OSS）
└── / → 静态页面（如有H5需求）

MySQL 8.0（3306端口，只监听本地）
Spring Boot（8080端口）
```

### 为什么不分开

| 方案 | 月费 | 适合场景 |
|------|------|---------|
| 单台 ECS 自建全部 | 80-100 | 本科毕设/小项目，够用 |
| ECS + RDS MySQL | 150-200+ | 生产环境，需要高可用 |
| 云函数 + Serverless | 按量计费 | 流量极低时便宜，但架构复杂 |

---

## 二、图片存储方案

### 阶段 1：先用服务器本地磁盘（免费）

```
服务器 /var/www/images/
├── items/
│   ├── uuid-1.jpg
│   └── uuid-2.png
└── avatars/
```

- 前端上传图片 → 后端接收 → 存到本地磁盘 → 返回访问 URL
- 优点：零成本，简单
- 缺点：服务器磁盘有限，无 CDN，换服务器需迁移

### 阶段 2：切换到阿里云 OSS（按量计费）

| 项目 | 费用 |
|------|------|
| 标准存储 | 0.12 元/GB/月 |
| 外网下行流量 | 0.25 元/GB |
| 请求次数 | 0.01 元/万次 |
| **100 张图片（共 50MB）** | **约 0.006 元/月** |

- 几乎免费，1000 张图片才几毛钱
- 前端直接访问 CDN URL，速度快
- 切换时只改 OSS 配置，后端代码不用改

### 切换方式

两种存储共用同一个接口，只改配置：

```yaml
# application.yml — 阶段 1（本地）
storage:
  type: local
  path: /var/www/images

# application.yml — 阶段 2（OSS）
storage:
  type: oss
  endpoint: https://oss-cn-hangzhou.aliyuncs.com
  access-key: ${OSS_ACCESS_KEY}
  bucket: your-bucket-name
```

---

## 三、域名与 HTTPS

### 域名（可选，推荐）

| 项目 | 费用 |
|------|------|
| .com 域名 | 阿里云约 55 元/年 |
| 阿里云学生域名 | 1 元/年（限 .top/.xyz） |
| DNS 解析 | 免费 |

### HTTPS（免费）

- 阿里云提供免费 SSL 证书（每个域名 20 个免费证书）
- Nginx 配置 HTTPS，小程序要求必须 HTTPS
- 或用 Let's Encrypt（免费自动续期）

**注意**：微信小程序要求后端必须是 HTTPS + 已备案域名，不能用 IP 直连。

### 备案（免费但耗时）

- 阿里云备案免费，需要 10-20 个工作日
- 需要有域名
- 个人备案准备身份证即可

---

## 四、向量搜索方案（后续）

### 最低成本方案：MySQL 存向量

不引入 Milvus，直接在 MySQL 的 `items` 表加一个 `embedding` 列（BLOB 类型存 float 数组）。

搜索时：
```java
// 从数据库加载所有向量，内存计算相似度（项目小够用）
List<float[]> vectors = itemMapper.getAllEmbeddings();
float similarity = cosineSimilarity(queryVector, itemVector);
```

- 500 个物品 × 512 维向量 = ~1MB，内存计算 < 1ms
- 零额外成本
- 物品超过 1 万再考虑 Milvus

### CLIP 模型部署

- 下载开源 CLIP 模型（~400MB）放在服务器上
- Java 通过 JNI 或启动 Python 子进程调用
- 或者发布物品时，前端直接调阿里云百炼 API 生成向量（按量计费）

---

## 五、部署步骤概览

```
1. 购买阿里云 ECS + 学生优惠
2. 域名注册 + 备案（可选，但小程序上线必须）
3. 服务器初始化（安装 JDK 17、MySQL、Nginx、Maven）
4. 后端打包 → 上传到服务器 → nohup 运行
5. Nginx 配置反向代理 + HTTPS
6. 小程序管理后台配置服务器域名白名单
7. 测试 → 上线
```

---

## 六、费用总结

| 项目 | 月费 | 备注 |
|------|------|------|
| ECS 2C4G | ~80 元 | 学生机可低至 9.9 元 |
| MySQL | 0（自建） | 含在 ECS 里 |
| 图片存储 | ~0 元 | 初期本地磁盘，几毛钱/月 |
| 域名 | ~5 元/月（均摊） | 首年 60 元 |
| HTTPS 证书 | 0 元 | 阿里云免费 |
| **总计** | **约 85-90 元/月** | 学生机可压到 **15 元/月** |

---

## 七、架构图（最终形态）

```
                    ┌─────────────┐
                    │  微信小程序   │
                    └──────┬──────┘
                           │ HTTPS
                    ┌──────▼──────┐
                    │    Nginx    │  ← 证书 / 反向代理
                    └──────┬──────┘
                           │
              ┌────────────┼────────────┐
              │            │            │
        ┌─────▼─────┐ ┌───▼────┐ ┌────▼─────┐
        │ Spring Boot│ │  MySQL  │ │ 图片/向量 │
        │  (:8080)   │ │ (:3306) │ │ OSS/本地  │
        └────────────┘ └────────┘ └──────────┘

              阿里云 ECS（1 台，2C4G）
```
